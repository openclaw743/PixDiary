# Backend API base URL (used by src/api/client.ts).
# In dev, the Vite proxy routes /api/* → http://localhost:3000, so leaving this
# blank is fine — fetch('/api/...') will be proxied. Set this for previews
# pointing at a deployed backend.
VITE_API_BASE_URL=
